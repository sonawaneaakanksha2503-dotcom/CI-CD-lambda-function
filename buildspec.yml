version: 0.2

phases:
  build:
    commands:
      - zip function.zip lambda_function.py
      - aws lambda update-function-code \
        --function-name serverless-contact \
        --zip-file fileb://function.zip